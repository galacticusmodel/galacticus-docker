# Docker-compose file to execute a Galacticus run
version: '3.3'

services:

   galacticus:
     image: galacticus/galacticus:latest
     container_name: galacticus
     entrypoint: /usr/local/galacticus/run.sh
     command: ["TREES_PER_DECADE=2", "STRIDE=2", "OFFSET=0"]
     volumes:
        - ./data:/data
  